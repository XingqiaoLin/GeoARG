interface:
  display_name: "Novel ARG watch"
  short_description: "Date-gate novel resistance genes"
  default_prompt: "Use $novel-arg-watch to search antimicrobial resistance genes after a cutoff date and cross-validate preprints before treating any gene as novel."
policy:
  allow_implicit_invocation: true
