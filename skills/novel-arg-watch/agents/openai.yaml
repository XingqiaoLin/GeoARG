interface:
  display_name: "Novel ARG watch"
  short_description: "Date-gate ARGs, verify experiments, and search RefSeq"
  default_prompt: "Use $novel-arg-watch to find ARGs after a cutoff date, search their RefSeq records, cross-validate earlier reports, and verify gene-level experimental evidence."
policy:
  allow_implicit_invocation: true
